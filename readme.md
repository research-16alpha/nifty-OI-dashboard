# 📈 Options Backtest Dashboard

This is a **public Streamlit dashboard** for viewing results of a large options backtest on NIFTY.

All backtests are **precomputed locally** (via `final_code.py`) into folders under:

